#!/usr/bin/env php
<?php

(function() {
    $container = require __DIR__ . '/../bootstrap.php';

    // trigger route configuration
    $container->get('app');

    // create caches
    $container->get('router')->createCaches();
})();
