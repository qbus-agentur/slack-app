#!/usr/bin/env php
<?php
include_once __DIR__ . '/../vendor/autoload.php';

$container = (new \Bnf\Di\Container([new \Qbus\SlackApp\Bootstrap]));

// trigger route configuration
$container->get('app');

// create caches
$container->get('router')->createCaches();
